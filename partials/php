export RULE_DEP_BUILDPHP=
export RULE_CMD_BUILDPHP=docker-compose -f $(COMPOSE_PATH) -p $(DOCKER_NAME) build --force-rm php

####################
# Build containers #
####################

## Build `php` container
build-php: $(RULE_DEP_BUILDPHP)
	$(RULE_CMD_BUILDPHP)